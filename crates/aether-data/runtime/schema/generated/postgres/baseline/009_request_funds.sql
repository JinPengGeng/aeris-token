-- Generated by aether-data-schema from crates/aether-data/runtime/schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS public.request_fund_reservations (
    reservation_token character varying(128) NOT NULL,
    request_id character varying(128) NOT NULL,
    attempt_id uuid,
    candidate_id character varying(128),
    provider_id character varying(128),
    provider_api_key_id character varying(128),
    model_id character varying(128),
    dispatched_at timestamp with time zone,
    terminal_facts jsonb,
    wallet_id character varying(64),
    quote jsonb NOT NULL,
    state character varying(32) NOT NULL,
    actual_cost_units bigint,
    collected_cost_units bigint DEFAULT 0 NOT NULL,
    reconciliation_facts jsonb,
    settlement jsonb,
    created_at timestamp with time zone DEFAULT now() NOT NULL,
    updated_at timestamp with time zone DEFAULT now() NOT NULL
);

ALTER TABLE ONLY public.request_fund_reservations ADD CONSTRAINT request_fund_reservations_pkey PRIMARY KEY (reservation_token);
CREATE INDEX IF NOT EXISTS request_fund_reservations_request_id_idx ON public.request_fund_reservations USING btree (request_id);
CREATE INDEX IF NOT EXISTS request_fund_reservations_wallet_state_idx ON public.request_fund_reservations USING btree (wallet_id, state);
ALTER TABLE ONLY public.request_fund_reservations ADD CONSTRAINT request_fund_reservations_wallet_id_fkey FOREIGN KEY (wallet_id) REFERENCES public.wallets(id) ON DELETE RESTRICT;

CREATE TABLE IF NOT EXISTS public.request_fund_allocations (
    reservation_token character varying(128) NOT NULL,
    ordinal integer NOT NULL,
    source_kind character varying(24) NOT NULL,
    source_id character varying(128) NOT NULL,
    usage_date character varying(10),
    quota_cost_units bigint,
    reserved_cost_units bigint NOT NULL,
    collected_cost_units bigint DEFAULT 0 NOT NULL
);

ALTER TABLE ONLY public.request_fund_allocations ADD CONSTRAINT request_fund_allocations_pkey PRIMARY KEY (reservation_token, ordinal);
CREATE INDEX IF NOT EXISTS request_fund_allocations_source_idx ON public.request_fund_allocations USING btree (source_id, source_kind, usage_date);
ALTER TABLE ONLY public.request_fund_allocations ADD CONSTRAINT request_fund_allocations_reservation_token_fkey FOREIGN KEY (reservation_token) REFERENCES public.request_fund_reservations(reservation_token) ON DELETE RESTRICT;

CREATE TABLE IF NOT EXISTS public.request_fund_recoveries (
    request_id character varying(128) NOT NULL,
    wallet_id character varying(64) NOT NULL,
    frozen_actual_cost_units bigint NOT NULL,
    prior_entitlement_cost_units bigint NOT NULL,
    collected_cost_units bigint DEFAULT 0 NOT NULL,
    updated_at timestamp with time zone DEFAULT now() NOT NULL
);

ALTER TABLE ONLY public.request_fund_recoveries ADD CONSTRAINT request_fund_recoveries_pkey PRIMARY KEY (request_id);
ALTER TABLE ONLY public.request_fund_recoveries ADD CONSTRAINT request_fund_recoveries_wallet_id_fkey FOREIGN KEY (wallet_id) REFERENCES public.wallets(id) ON DELETE RESTRICT;

CREATE TABLE IF NOT EXISTS public.request_fund_collection_receipts (
    id character varying(64) NOT NULL,
    request_id character varying(128) NOT NULL,
    collected_cost_units bigint NOT NULL,
    recharge_before double precision NOT NULL,
    recharge_after double precision NOT NULL,
    gift_before double precision NOT NULL,
    gift_after double precision NOT NULL,
    created_at timestamp with time zone DEFAULT now() NOT NULL
);

ALTER TABLE ONLY public.request_fund_collection_receipts ADD CONSTRAINT request_fund_collection_receipts_pkey PRIMARY KEY (id);
CREATE INDEX IF NOT EXISTS request_fund_collection_receipts_request_idx ON public.request_fund_collection_receipts USING btree (request_id);
ALTER TABLE ONLY public.request_fund_collection_receipts ADD CONSTRAINT request_fund_collection_receipts_request_id_fkey FOREIGN KEY (request_id) REFERENCES public.request_fund_recoveries(request_id) ON DELETE RESTRICT;

