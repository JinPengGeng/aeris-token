-- Generated by aether-data-schema from crates/aether-data/runtime/schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS public.usage_daily_cost_contributions (
    request_id character varying(128) NOT NULL,
    usage_id character varying(36) NOT NULL,
    user_id character varying(128),
    api_key_id character varying(128),
    api_key_is_standalone boolean NOT NULL,
    attempt_funds boolean NOT NULL,
    actual_cost_units bigint NOT NULL,
    accounting_at timestamp with time zone,
    updated_at timestamp with time zone DEFAULT now() NOT NULL
);

ALTER TABLE ONLY public.usage_daily_cost_contributions ADD CONSTRAINT usage_daily_cost_contributions_pkey PRIMARY KEY (request_id);

