# Upstream synchronization refresh, 2026-09-17

Issues #446 and #448 describe two failed synchronization attempts against old
fork main `4490f7987`. The new #449 alert (2026-09-17 08:37:44 UTC) records
upstream `681ce56c4f27` against fork main `fb4d9a93f`. Treat all three as one
integration track.

#449 reports three conflict paths: Gateway video routing, usage runtime and
video-core OpenAI. The seven-path inventory below compares the integrated
branch `26638a4f7` against that same upstream; these are different bases, so
the two conflict counts do not contradict one another.

The current GitHub heads are:

- Fork main: `fb4d9a93fed324ef93e79e32a8e4e957adba1d4b`.
- Upstream main: `681ce56c4f27ae1f718c2be328fe3ed98f0ee85b`
  (`Merge pull request #831 from stabey/fix/gemini-native-search`).
- Current integrated branch: `26638a4f72295b7f5cf4851a331efce7b2b5314b`.

An object-only fetch followed by
`git merge-tree --write-tree --name-only HEAD 681ce56c4f27ae1f718c2be328fe3ed98f0ee85b`
returned conflict status 1. No branch, index or working file was changed. The
seven conflicted paths are:

1. `apps/aether-gateway/src/tests/video/routing.rs`
2. `apps/aether-gateway/src/tests/video/xai.rs` (add/add)
3. `crates/aether-ai/formats/src/formats/openai/chat/stream.rs`
4. `crates/aether-ai/formats/src/formats/openai/responses/mod.rs`
5. `crates/aether-video-tasks-core/src/openai.rs`
6. `crates/aether-video-tasks-core/src/snapshot.rs`
7. `crates/aether-video-tasks-core/src/sync.rs`

The old upstream revisions `e9899200f8d9` and `364692da55eb`, and the existing
local resolution branch tip `5393fac37`, are not ancestors of current HEAD.
The old three-path alert is therefore neither a current conflict inventory nor
proof that the local resolution branch has been integrated.

This check compares committed trees. The current uncommitted video revision,
funding, recharge, restore and release work was not part of the merge-tree
input. Integration must preserve and separately reconcile that work; applying
the old resolution blindly would not establish correctness.

The next integration slice should prepare resolutions in an isolated copy,
preserve the current video revision/fencing and successful-upstream accounting
contracts, and retain both protocol behavior and xAI/routing coverage. Validate
the video and async suites, OpenAI stream/Responses format regressions and
lockfile consistency before requesting or executing a history-changing step.
No merge commit or remote submission has been made by this refresh.

Temporary resolution drafts now cover all seven paths. The OpenAI formats
draft combines reasoning/citation handling with the Responses call-ID changes.
The video-core draft fills ordinary OpenAI duration handling while retaining
terminal guards; snapshot and sync resolve to the current working versions.
The Gateway test draft preserves both native/compat behavior and existing
authentication/no-dispatch assertions. The drafts have local formatting and
patch-application checks, but the full merged candidate has not been integrated
or tested. They are retained in `upstream-format-resolution-draft`,
`upstream-video-resolution-draft` and `upstream-video-tests-resolution-draft`
under the task log root.

## Complete isolated candidate

`upstream-candidate/` now contains the full automatic merge tree, all seven
resolved paths and the current uncommitted changes. Local changes to usage
queue/runtime were overlaid through a three-way merge without conflicts;
other preserved local changes were copied only after baseline verification.
The preparation manifest records 196 source paths and their hashes.

The incoming tree has 33 changed paths relative to the integrated HEAD. One is
a generated Windsurf test fixture containing only the eleven bytes `test binary`;
that fixture is excluded from the candidate and retained only as evidence.
This content integration does not create a merge commit or alter the main
working tree's branch/index.

Initial candidate tests passed: formats 971, Antigravity 18, video core 54,
usage runtime 379, ordinary PostgreSQL usage 113 (12 live cases remain ignored
by that filter), Gateway video and async, and the complete 62 PostgreSQL / 24
Gateway inventories. Frontend changes passed 30 tests, type checking and
targeted lint. Static checks are still running at this checkpoint.

Independent review identified two protocol gaps not covered by that first
passing matrix: empty reasoning fields can hide a populated fallback field,
and multipart Gemini citations can bind to the wrong text part. The citation
fix also needs to preserve offsets when Responses combines text blocks.
Corrections and semantic regressions are being prepared before adoption.
That initial checkpoint was followed by the corrections and final adoption
recorded below; it is retained to distinguish reviewed findings from executed
regressions.

Evidence is in `upstream-candidate-evidence/` under the task log root, including
`preparation.json`, `rust-summary.json` and `frontend-summary.json`.

## Verified local adoption

The complete candidate, including the protocol corrections, passed final
validation and was applied to 31 working-tree paths. Every applied file's
SHA-256 equals the tested candidate. All 163 other previously changed paths
were preserved, including later local documentation edits. HEAD remains
`26638a4f7` and the index is unchanged; no commit, push or remote synchronization
was performed. Live read-only checks reconfirmed the same fork/upstream heads.

The corrections preserve populated reasoning aliases after empty/null fields,
Gemini source part indices and UTF-8 span boundaries, and target annotation
offsets when Responses coalesces text. Stream citations follow actual emitted
text order, including cumulative/delta frames and signature-only metadata.
Unmatchable or noncontiguous spans are dropped instead of attributed to other
text; native Gemini metadata remains available. Terminal-only observers retain
no new content buffers.

Final evidence:

- Formats: 979 passed, including eight new semantic regressions.
- Usage runtime: 379 passed; PostgreSQL usage unit filter: 113 passed, 13 ignored.
- Gateway video: 115 passed; async filter: 30 passed.
- Required PostgreSQL inventory: 63 exact targets, each one passed/non-ignored.
  The new target persists four distinct body-capture states and checks all four
  fields through request-ID, ID and batch-ID reads against real PostgreSQL.
- Required Gateway inventory: 24 exact targets, each one passed/non-ignored.
- Strict Gateway and affected upstream-crate Clippy, formatting, diff and the
  PostgreSQL harness fixture passed. Cargo.lock remained unchanged.
- Frontend: 30 tests, type checking and targeted lint passed. Unchanged provider
  Antigravity and video-core slices passed 18/54 in the initial candidate run.

`upstream-candidate-evidence/final/` retains the final logs and summaries.
`adoption-manifest.json` records the before/after hashes and patch;
`adoption-verified.json` proves application and preservation checks. This closes
the local content-integration slice. The GitHub sync issues still require the
eventual history/remote operation and its hosted checks.

Full command evidence is retained at
`/Users/jinpeng/.agents/tmp/aeris-followup-20260917/upstream-681ce56-merge-tree.txt`.
